import { Product } from '../../models/products';


/* 
## Product data scheme

export type Product = {
    id?: number;
    name: string;
    price: number;
    category?: string;
};
*/
describe('Product Handler', () => {

    /* 
      check if the methods in the product handlers are functioning
      as expected
    */
    it('create request with playload should return correct response', async () => {

    });
});
